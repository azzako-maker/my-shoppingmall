# 어드민 상품 등록 가이드

## 소개

이 쇼핑몰 MVP에서는 별도의 어드민 페이지를 구현하지 않습니다. 대신 **Supabase Dashboard**를 통해 직접 상품을 등록하고 관리합니다.

이 가이드는 기술 지식이 없는 분도 쉽게 따라할 수 있도록 단계별로 작성되었습니다.

---

## 사전 준비

### Supabase 프로젝트 접근

1. [Supabase Dashboard](https://supabase.com/dashboard)에 접속하여 로그인합니다.
2. 등록된 프로젝트를 선택합니다.
3. 좌측 메뉴에서 **"Table Editor"**를 클릭합니다.
4. 테이블 목록에서 **"products"** 테이블을 선택합니다.

> 💡 **팁**: 처음 사용하시는 경우, Table Editor 메뉴가 보이지 않을 수 있습니다. 이 경우 좌측 메뉴를 펼쳐서 찾아보세요.

---

## 상품 등록 단계별 가이드

### Step 1: 테이블 선택

Supabase Dashboard에서 다음 경로로 이동합니다:

```
Dashboard → Table Editor → products
```

"products" 테이블을 클릭하면 현재 등록된 상품 목록이 표시됩니다.

### Step 2: 새 상품 추가

1. 상단의 **"Insert row"** 또는 **"New row"** 버튼을 클릭합니다.
2. 새 행이 추가되며 각 필드를 입력할 수 있는 폼이 나타납니다.

### Step 3: 필수 필드 입력

다음 필드들을 입력합니다:

#### 필수 입력 필드

**1. name (상품명)**
- 필수 입력 사항입니다.
- 예시: `무선 블루투스 이어폰`
- 빈 값으로 남겨둘 수 없습니다.

**2. price (가격)**
- 필수 입력 사항입니다.
- 숫자만 입력합니다. (단위: 원)
- 예시: `89000`
- ⚠️ 마지막에 "원" 기호를 붙이지 않습니다.
- ⚠️ 0 이상의 숫자만 입력 가능합니다. (음수 불가)

#### 선택 입력 필드

**3. description (상품 설명)**
- 선택 입력 사항입니다.
- 상품의 상세 설명을 입력합니다.
- 예시: `고음질 노이즈 캔슬링 기능, 30시간 재생`
- 빈 값으로 남겨둘 수 있습니다.

**4. category (카테고리)**
- 선택 입력 사항입니다.
- 상품의 카테고리 ID를 입력합니다.
- 예시: `electronics`
- ⚠️ **중요**: 카테고리 표시명(예: "전자제품")이 아닌 **카테고리 ID**(예: "electronics")를 입력해야 합니다.
- ⚠️ 대소문자를 정확히 입력해야 합니다.
- 카테고리 목록은 [하단 참고](#카테고리-목록)
- 빈 값으로 남겨둘 수 있습니다. (카테고리 없음)

**5. stock_quantity (재고 수량)**
- 선택 입력 사항입니다.
- 현재 재고 수량을 입력합니다.
- 예시: `150`
- 기본값: `0` (품절 상태)
- ⚠️ 0 이상의 정수만 입력 가능합니다. (소수점 불가, 음수 불가)
- 0을 입력하면 상품 상세 페이지에서 "품절"로 표시됩니다.

**6. is_active (판매 활성화)**
- 선택 입력 사항입니다.
- 상품을 판매 중인지 여부를 설정합니다.
- 값: `true` (판매 중) 또는 `false` (판매 중지)
- 기본값: `true`
- ⚠️ `false`로 설정하면 상품 목록에 표시되지 않습니다.
- 판매를 일시 중단하고 싶을 때 사용합니다.

#### 자동 생성 필드 (수정 불가)

다음 필드들은 자동으로 생성되므로 수정할 필요가 없습니다:

- **id**: 상품 고유 식별자 (UUID, 자동 생성)
- **created_at**: 상품 등록 일시 (자동 생성)
- **updated_at**: 상품 수정 일시 (자동 생성)

### Step 4: 데이터 저장

1. 모든 필수 필드를 입력했습니다.
2. 우측 상단 또는 하단의 **"Save"** 또는 **"Insert"** 버튼을 클릭합니다.
3. 성공 메시지가 표시되면 상품 등록이 완료됩니다.

> ✅ **완료**: 등록한 상품은 즉시 웹사이트의 상품 목록에 표시됩니다.

---

## 필수 필드 상세 설명

### 필수 필드

#### `name` (TEXT, NOT NULL)

**설명**: 상품의 이름

**예시 값**:
- `무선 블루투스 이어폰`
- `면 100% 기본 티셔츠`
- `클린 코드`

**제약 사항**:
- ✅ 필수 입력
- ✅ 빈 값 불가
- ✅ 텍스트 입력 가능

---

#### `price` (DECIMAL(10,2), NOT NULL, CHECK >= 0)

**설명**: 상품 가격 (원 단위)

**예시 값**:
- `89000`
- `32000.50`
- `25000`

**제약 사항**:
- ✅ 필수 입력
- ✅ 0 이상의 숫자만 입력 가능
- ✅ 소수점 2자리까지 입력 가능
- ❌ 음수 입력 불가
- ❌ "원" 기호 포함 불가

---

### 선택 필드

#### `description` (TEXT, NULL)

**설명**: 상품의 상세 설명

**예시 값**:
- `고음질 노이즈 캔슬링 기능, 30시간 재생`
- `심플한 디자인, 5가지 컬러`
- `소프트웨어 장인 정신의 바이블`

**제약 사항**:
- ⚪ 선택 입력
- ⚪ 빈 값 가능 (null)
- ⚪ 텍스트 입력 가능 (길이 제한 없음)

---

#### `category` (TEXT, NULL)

**설명**: 상품 카테고리 ID

**예시 값**:
- `electronics`
- `clothing`
- `books`

**제약 사항**:
- ⚪ 선택 입력
- ⚪ 빈 값 가능 (null)
- ✅ **중요**: 카테고리 ID를 정확히 입력해야 함 (대소문자 구분)
- ✅ 유효한 카테고리 ID만 사용 가능
- ❌ 카테고리 표시명 입력 불가 (예: "전자제품" ❌)

**사용 가능한 카테고리 목록**: [카테고리 목록 참고](#카테고리-목록)

---

#### `stock_quantity` (INTEGER, DEFAULT 0, CHECK >= 0)

**설명**: 재고 수량

**예시 값**:
- `150`
- `80`
- `0` (품절)

**제약 사항**:
- ⚪ 선택 입력 (기본값: 0)
- ✅ 0 이상의 정수만 입력 가능
- ❌ 음수 입력 불가
- ❌ 소수점 입력 불가
- ⚠️ 0을 입력하면 상품 상세 페이지에서 "품절"로 표시됩니다.

---

#### `is_active` (BOOLEAN, DEFAULT true)

**설명**: 상품 판매 활성화 여부

**값**:
- `true`: 판매 중 (상품 목록에 표시됨)
- `false`: 판매 중지 (상품 목록에 표시되지 않음)

**제약 사항**:
- ⚪ 선택 입력 (기본값: true)
- ✅ true 또는 false만 입력 가능

**사용 시나리오**:
- 판매 중단된 상품을 숨기고 싶을 때: `false`로 설정
- 재판매 시작할 때: `true`로 변경

---

## 카테고리 목록

다음 카테고리 ID를 사용할 수 있습니다:

| 카테고리 ID | 표시명 | 설명 | 예시 상품 |
|------------|--------|------|-----------|
| `electronics` | 전자제품 | 전자기기, 액세서리 | 블루투스 이어폰, 스마트워치 |
| `clothing` | 의류 | 옷, 신발, 액세서리 | 티셔츠, 청바지, 후드집업 |
| `books` | 도서 | 책, 전자책 | 개발서, 소설 |
| `food` | 식품 | 음식, 음료 | 커피, 견과류, 오일 |
| `sports` | 스포츠 | 운동용품 | 요가매트, 덤벨 |
| `beauty` | 뷰티 | 화장품, 스킨케어 | 세럼, 선크림 |
| `home` | 생활/가정 | 가정용품 | 디퓨저, 수납함 |

**⚠️ 중요 사항:**

1. **카테고리 ID는 정확히 입력해야 합니다** (대소문자 구분)
   - ✅ 올바른 예: `electronics`, `clothing`
   - ❌ 잘못된 예: `Electronics`, `CLOTHING`, `전자제품`

2. **카테고리 표시명이 아닌 ID를 입력합니다**
   - ✅ 올바른 예: `electronics`
   - ❌ 잘못된 예: `전자제품`

3. **존재하지 않는 카테고리 ID를 입력하면** 카테고리 필터에서 표시되지 않습니다.

4. **카테고리 없이 등록 가능합니다** (빈 값 또는 null 입력)

---

## 주의사항 및 실수 방지

### 가격 입력 시 주의사항

❌ **잘못된 입력:**
- `89,000` (쉼표 포함)
- `89000원` ("원" 기호 포함)
- `-1000` (음수)
- `무료` (텍스트)

✅ **올바른 입력:**
- `89000`
- `89000.50` (소수점 가능)

---

### 카테고리 입력 시 주의사항

❌ **잘못된 입력:**
- `Electronics` (대문자 시작)
- `ELECTRONICS` (전부 대문자)
- `전자제품` (표시명)
- `전자제품-electronics` (혼합)

✅ **올바른 입력:**
- `electronics` (정확한 카테고리 ID, 소문자)

**확인 방법**: 카테고리를 입력한 후, 웹사이트의 카테고리 필터에서 해당 상품이 표시되는지 확인하세요.

---

### 재고 수량 입력 시 주의사항

❌ **잘못된 입력:**
- `-10` (음수)
- `50.5` (소수점)
- `재고 있음` (텍스트)

✅ **올바른 입력:**
- `150`
- `0` (품절 상태)

---

### 상품 활성화 주의사항

- `is_active`가 `false`로 설정되어 있으면 상품 목록에 표시되지 않습니다.
- 상품이 목록에 나타나지 않는다면 `is_active` 값이 `true`인지 확인하세요.

---

## 데이터 수정 및 삭제

### 상품 정보 수정

1. Supabase Dashboard → Table Editor → products 테이블
2. 수정하고 싶은 상품의 행을 클릭합니다.
3. 필드를 수정합니다.
4. **"Save"** 또는 **"Update"** 버튼을 클릭합니다.

> ⚠️ **주의**: `id`, `created_at`, `updated_at` 필드는 자동 생성되므로 수정할 수 없습니다.

---

### 상품 삭제

1. Supabase Dashboard → Table Editor → products 테이블
2. 삭제하고 싶은 상품의 행을 선택합니다.
3. 삭제 버튼을 클릭합니다. (일반적으로 행 오른쪽에 있음)
4. 확인 대화상자에서 삭제를 확인합니다.

**⚠️ 주의사항:**

- 삭제된 상품은 **복구할 수 없습니다**.
- 이미 주문된 상품을 삭제하려고 하면 오류가 발생할 수 있습니다. (참조 무결성)
- 상품을 완전히 삭제하기보다는 `is_active`를 `false`로 설정하여 숨기는 것을 권장합니다.

---

## FAQ (자주 묻는 질문)

### Q1. 상품을 등록했는데 웹사이트에 표시되지 않아요.

**A**: 다음을 확인해보세요:
1. `is_active` 필드가 `true`로 설정되어 있는지 확인
2. 브라우저를 새로고침해보세요
3. 웹사이트 개발 서버가 실행 중인지 확인

---

### Q2. 카테고리를 입력했는데 필터에서 나타나지 않아요.

**A**: 다음을 확인해보세요:
1. 카테고리 ID가 정확히 입력되었는지 확인 (대소문자 구분)
2. [카테고리 목록](#카테고리-목록)에 있는 ID만 사용 가능
3. 카테고리 표시명(예: "전자제품")이 아닌 ID(예: "electronics")를 입력했는지 확인

---

### Q3. 가격에 소수점을 입력해도 되나요?

**A**: 네, 가능합니다. 예: `89000.50`
단, 일반적으로 원화 가격은 정수로 입력하는 것을 권장합니다.

---

### Q4. 여러 개의 상품을 한 번에 등록할 수 있나요?

**A**: Supabase Dashboard에서는 하나씩 등록해야 합니다.
대량 등록이 필요한 경우, 개발자에게 CSV 가져오기 기능 구현을 요청하거나, 개발자 도구를 통해 직접 데이터를 삽입할 수 있습니다.

---

### Q5. 상품 설명이 너무 길면 문제가 되나요?

**A**: 아니요, 문제되지 않습니다. 다만, 너무 긴 설명은 상품 카드에서 잘릴 수 있습니다. 핵심 정보를 앞부분에 배치하는 것을 권장합니다.

---

### Q6. 카테고리를 나중에 변경할 수 있나요?

**A**: 네, 가능합니다. 상품 정보 수정 방법을 참고하여 `category` 필드를 수정하면 됩니다.

---

### Q7. 재고 수량을 실시간으로 업데이트해야 하나요?

**A**: MVP 단계에서는 수동 업데이트가 필요합니다. 주문이 발생할 때마다 Supabase Dashboard에서 재고를 수동으로 감소시켜야 합니다. (향후 자동 업데이트 기능 추가 예정)

---

## 문제 해결

### 오류 메시지가 나타날 때

**"name cannot be null"**
- `name` 필드를 입력했는지 확인하세요.

**"price must be >= 0"**
- 가격이 음수가 아닌지 확인하세요.

**"stock_quantity must be >= 0"**
- 재고 수량이 음수가 아닌지 확인하세요.

**"foreign key constraint"**
- 이미 주문된 상품을 삭제하려고 할 때 발생할 수 있습니다. 삭제 대신 `is_active`를 `false`로 설정하세요.

---

## 추가 자료

- [Supabase 공식 문서](https://supabase.com/docs/guides/database/tables)
- [Supabase Table Editor 가이드](https://supabase.com/docs/guides/database/tables#table-editor)

---

## 도움말

문제가 발생하거나 질문이 있으시면 개발팀에 문의해주세요.

---

**마지막 업데이트**: 2025-01-XX

